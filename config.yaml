# config.yaml

rules:
  naming_convention:
    tool: flake8
    threshold: 0

  complexity:
    tool: radon
    threshold: 0

  security:
    tool: bandit
    threshold: 0

ai_review:
  temperature: 0.2
  max_comments: 10
  min_confidence: 0.7

auto_reject:
  enabled: false
  overall_threshold: 0
